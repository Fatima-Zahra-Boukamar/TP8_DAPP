name: hello_world_dapp
description: A Hello World Decentralized Application (DApp)

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # DÉPENDANCES BLOCKCHAIN - VERSIONS COMPATIBLES
  provider: ^6.0.5
  web3dart: ^2.6.1      # Version compatible
  http: ^0.13.6
  web_socket_channel: ^2.4.4

  # Icônes
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # ASSETS - TRÈS IMPORTANT !
  assets:
    - src/artifacts/HelloWorld.json